#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <owi.h>

int test2(char *buf){/* ... */}

int main(int argc, char const *argv[])
{
	char arr[10];
	for (int i = 0; i < 10; i++)
	{
		arr[i] = owi_char();
	}
	owi_assume(arr[9] == '\0');

	test2(arr);
	return 0;
}
